# Setup
```sh
# create directory were bindings would be
mkdir external && cd external
# download bindings
wget https://github.com/ianprime0509/zig-gobject/releases/download/v0.2.2/bindings-gnome46.tar.zst
# decompress bindings
tar xf bindings-gnome46.tar.zst
```